<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.soft.dao.MemberDAO">  
  
<!--   	<resultMap id="MemberVOResultMap" type="MemberVO">
  		<result property="me_id" column="me_id"/>
  		<result property="me_pwd" column="me_pwd"/>
  		<result property="me_name" column="me_name"/>
  		<result property="me_email" column="me_email"/>
  		<result property="me_regdate" column="me_regdate"/>
  		<result property="me_image" column="me_image"/>
  	</resultMap>

	<update id="keepLogin">
		update tbl_member
		
		
	</update>   -->
  
  	<!-- 회원 가입 -->
  	<insert id="register">
  		insert
		 into
		tbl_member
		(
			me_id		
		 ,	me_email
		 ,  me_name
		 ,	me_pwd
		 ,	me_regdate
		)
		values
		(
			#{me_id}
		,	#{me_email}
		,   #{me_name}
		,	#{me_pwd}
		,	now()
		)
  	</insert>
  	
  	<!-- 아이디 중복 체크 -->
  	<select id="idChk" resultType="int">
  		select 
  		 count(*)
  		from 
  		 tbl_member
  		where me_email = #{me_email}
  	</select>
  	
  	<!-- 비밀번호 체크 -->
  	<select id="passChk" resultType="int">
  		select 
  		 count(*)
  		from 
  		 tbl_member
  		where me_email = #{me_email}
  		and   me_pwd   = #{me_pwd}
  	</select>
  	
	
	<!-- 로그인 확인 -->
	<select id="login" resultType="memberVO">
		select 
		   *
		from
		tbl_member
		where me_email = #{me_email} 
		and me_pwd = #{me_pwd}
	</select>
	
	<select id="memberInfoSearch" parameterType="memberVO" resultType="memberVO">
	<![CDATA[
		select
		 *
		from 
			tbl_member
		where 
			me_id = #{me_id}
	]]>
	</select> 
	
	<!-- 비밀번호만 변경 -->
	<update id="memberUpdate" parameterType="memberVO">
			update tbl_member
			set
			me_pwd = #{me_pwd} 
			where me_id = #{me_id}
	</update>
	
	
	<!-- 회원관리 -->
	<select id="memberManage" resultType="memberVO" parameterType="memberVO">
	select * from tbl_member
	</select>
	
	<!-- 회원탈퇴 -->
	<delete id="memberDelete" parameterType = "int">
		delete from tbl_member where me_id = #{me_id}
	</delete>
	
	<update id="updateImg">
		update tbl_member set me_image = #{me_image} where me_id = #{me_id}
	</update>
	
	<!-- 관리자 지정 -->
	<update id="selectManage">
	update tbl_member set adminCk = '1' where me_id = #{me_id}
	
	</update>
	
	<insert id="insertAccount">
		insert into tbl_member
		(
			me_id
		,	me_email
		,	me_pwd
		,	adminCk
		)
			values
		(
			#{me_id}
		,	#{me_email}
		,	#{me_pwd}
		,	#{adminCk}
		)
	</insert>
</mapper>