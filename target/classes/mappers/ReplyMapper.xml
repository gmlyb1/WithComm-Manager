<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.soft.dao.ReplyDAO">

	<select id="replyList" parameterType="int" resultType="replyVO">
		select
			a.reply_no
		,	a.board_no
		,	a.reply_writer
		,	a.reply_content
		,	a.reply_regdate
		,	b.board_no
		from tbl_reply a left outer join tbl_board b
		on a.board_no = b.board_no
			<!-- where board_no = #{board_no} -->
	</select>

	<insert id="replyWrite" parameterType="com.soft.vo.replyVO">
		insert into tbl_reply(board_no, reply_writer, reply_content,reply_regdate)
			value(#{board_no},#{reply_writer},#{reply_content},CURRENT_TIMESTAMP)	
	</insert>

	<update id="replyModify" parameterType="replyVO">
		update tbl_reply set
			reply_writer = #{reply_writer}
		,	reply_content = #{reply_content}
		where reply_no = #{reply_no}
		and	board_no = #{board_no}
	</update>
	
	<delete id="replyDelete" parameterType="replyVO">
		delete from tbl_reply
		where reply_no = #{reply_no}
		and board_no = #{board_no}
	</delete>
</mapper>  