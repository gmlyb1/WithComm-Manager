<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.soft.service.impl.Accountmapper">  
  
  <select id="getIdCnt" parameterType="com.soft.vo.memberVO" resultType="java.lang.Integer">
       select
       	count(*)
       from
        tbl_member
       where 
        me_id = #{me_id}
  </select>
  
  <select id="actionLogin" parameterType="com.soft.vo.memberVO" resultType="com.soft.vo.memberVO">
  		select
  		 * 
  		from
  			tbl_member
  		where
  			me_id = #{me_id}
  		and
  			me_pwd = #{me_pwd} 
  </select>
  
 <update id="updateLastLogin" parameterType="com.soft.vo.memberVO">
  	update
  		tbl_member
	set
		me_latest_login = sysdate()
	where
		me_id=#{me_id}
  </update>
   
  
  <insert id="insertMember">
  		insert into tbl_member
  		   (
			 me_name
			,me_id
			,me_pwd
			,me_auth
			,me_email
			,me_tel
			,me_latest_login
			,me_delete_yn
			,me_regDate
  		   )
  			values
  		   (
	  		 #{me_name}
			,#{me_id}  		
	  		,#{me_pwd}
	  		,#{me_auth}
	  		,#{me_email}
	  		,#{me_tel}
	  		,#{me_latest_login}
	  		,#{me_delete_yn}
	  		,null
  		   )
  </insert>
  
  
  
  
  
  
  
  
</mapper>