<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.soft.dao.VisitCountDAO">  

	<insert id="insertVisitor" parameterType="VisitCountVO">
		INSERT INTO TBL_VISIT
		(
			VISIT_ID
		,	VISIT_IP
		,	VISIT_TIME
		,	VISIT_REFER
		,	VISIT_AGENT
		)
		VALUES
		(
			#{VISIT_ID},
			#{VISIT_IP},
			SYSDATETIME,
			<if test="visit_refer != null and visit_refer !=''">
			#{visit_refer},
			</if>
			#{VISIT_AGENT}
		)
	</insert>
	
	<select id="selectVisitList" resultType="VisitCountVO" parameterType="VisitCountVO">
		select
		 	a.*
		from
		 tbl_visit a
		 WHERE 
		 	1=1
		 <if test="visit_ip != null and visit_ip != ''">
	       AND visit_ip = #{visit_ip}
		  </if>
		    <if test="startDate != null and endDate != null and startDate != '' and endDate != ''">
	        AND regDate BETWEEN #{startDate} AND #{endDate}
	      </if>
	</select>

	<select id="visitAprvSttsCnt" parameterType="VisitCountVO" resultType="VisitCountVO">
		SELECT COUNT(visit_id) as visitAprvCnt
			FROM tbl_visit 
		WHERE DATE(regdate) = CURDATE()
	</select>
	
	
	<select id="selectVisitCount" parameterType="VisitCountVO" resultType="VisitCountVO">
	<![CDATA[
	SELECT 
    COALESCE((SELECT SUM(visitCount)
              FROM tbl_visit
              WHERE regDate >= CURDATE() AND regDate < CURDATE() + INTERVAL 1 DAY), 0) AS dayCnt,
    COALESCE((SELECT SUM(visitCount)
              FROM tbl_visit
              WHERE regDate >= CURDATE() - INTERVAL 7 DAY AND regDate < CURDATE()), 0) AS weekCnt,
    COALESCE((SELECT SUM(visitCount)
              FROM tbl_visit
              WHERE regDate >= CURDATE() - INTERVAL 1 MONTH AND regDate < CURDATE()), 0) AS monthCnt,
    COALESCE((SELECT SUM(visitCount)
              FROM tbl_visit
              WHERE regDate >= CURDATE() - INTERVAL 1 YEAR AND regDate < CURDATE()), 0) AS yearCnt
	]]>
	</select>

</mapper>